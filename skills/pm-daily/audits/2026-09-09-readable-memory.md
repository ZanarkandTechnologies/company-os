---
skill: pm-daily
date: 2026-09-09
change_type: behavior
owner: skill-maintenance
status: pass
review_route: reviewer
eval_required: yes
---

# Readable, evidence-selected Project Memory

## Change

- Before: ID-heavy snapshot examples, output boilerplate, mandatory headings,
  broad missing-field follow-ups, weak decision/SOP gates, no extraction JSON.
- After: descriptive source links, distinct substantive facts, precise evidence
  limitations, source-body consequential decisions, explicit file transformations,
  and one extraction JSON rendered into readable memory.
- Preserve: exact provider routing, supplied identity/status/acceptance metadata,
  valid memory history, local-only skill effects, and bounded Project ownership.
- Tradeoff: extraction adds one inspectable local artifact per Project/run.
- Weekly reads frozen extraction references for exact identity/workflow grouping;
  missing provenance blocks the affected grouping rather than guessing.

## Proof boundary

- Reuse the two previously collected Project caches in isolated Hermes runs.
- Add portable synthetic positive/negative evidence and existing-memory fixtures.
- Inspect actual memory, extraction, draft contents, and local reference targets.
- Keep private caches, model traces, and generated outputs outside the repository.
- No provider fetching, propagation, live installation, or scheduling in replay.

## Review findings and repairs

- Preserve memory bytes on no-op; return standalone extraction without appending
  an otherwise unnecessary extraction reference.
- Require supplied machine provenance and explicitly load it in Weekly.
- First replay exposed input-relative links copied into output directories;
  require output-relative or absolute targets and verify anchors.
- First replay repeated synthetic context prose; label wholly synthetic operating
  evidence in the title, including when Project identity itself is real.
- Second replay exposed repeated blocker categories and unverified Done claims
  treated as outcomes; enforce one fact/category and actual artifact evidence.
- Third replay exposed invented sub-anchors and missing readable empty-input
  limitations; reuse manifest anchors and distinguish initialization from no-op.

## Validation

- Source diff whitespace check: pass.
- Farplane eval lint: pass; this is static validation, not behavior proof.
- Shared skill-system validator: unavailable because its installed script cannot
  locate a Farplane repository root. This repository owns a standalone Hermes
  package, not a Farplane generated registry; no global registry was changed.
- Four final cases passed targeted semantic review: both historical Project
  caches, positive decision/SOP plus history, and insufficient context.
- Final inventory: four extraction JSONs, four memories, three precise drafts.
- Thirty local reference/anchor checks passed; no raw IDs in readable prose;
  draft routing IDs match filenames.
- Independent reviewer: bounded pass on all four cases; no remaining semantic
  blocker. Full Weekly execution and provider propagation were not evaluated.
- Unchanged-input Hermes repeat: memory bytes identical to the preceding result;
  no message drafts. Five targeted behavior checks passed in the final pass.
- No live profile installation was performed; source-path replay exercised the
  edited package. Broader workspace installation still follows its approval gate.
