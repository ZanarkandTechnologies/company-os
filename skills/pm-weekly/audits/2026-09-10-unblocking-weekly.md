---
skill: pm-weekly
date: 2026-09-10
change_type: behavior
owner: skill-maintenance
status: verified
review_route: reviewer
eval_required: yes
---

# Weekly unblocking and lean health

## Contract

- Before: skill-owned Markdown, paragraph-heavy rules, employee timing columns,
  technical receipt footer, and incomplete collection-health inputs.
- After: one extraction JSON; automation renders and applies authorized artifacts.
- People evidence retains accepted outputs and prioritizes removing supported
  dependencies beyond reminders; no worker ranking or speed-baseline comparison.
- Preserve approved SOP baselines and comparable evidence; promotion still needs
  explicit approval, not one fast sample.
- Health is optional: one short issue → consequence → fix bullet per affected
  Project; quiet successful collection is not failure.
- Freeze existing coverage/cache references; never infer health from empty sections.
- Keep JSON as the semantic authority, templates as layout, and side effects at
  the automation boundary. No runtime code, parallel control layer or new test suite.

## Proof

- Run the current skill in a fresh local lane against frozen synthetic blocked,
  sparse and quiet Project evidence; separately run a missing-Project case.
- Run Step 4 locally on resulting JSON; verify source links, preservation,
  complete report chain, no-op repeat and no provider actions.
- Independent review checks non-reminder intervention, no employee blame,
  concise material health, unchanged approved baseline, and honest coverage.
- Store generated evidence only in the private runtime; no live installation,
  source-provider reads, publishing or deployment is part of this run.

## Results

- Later user review exposed missing Person progress and visible per-Department
  executive coverage. The checks below proved the narrower unblocking/rendering
  behavior, not the full report contract; see `2026-09-10-report-coverage.md`.

- Operated Hermes extraction on three frozen synthetic Projects; 11 intended
  artifacts cover Project → Department → Company, Person/SOP memory, next-week
  memory and the complete executive draft.
- Source-driven corrections fixed ambiguous citations, repeated carry-forward,
  unsupported missing-process inference and dropped acceptance metadata.
- Independent semantic review passed: existing decision authority preserved,
  non-reminder intervention proposed, no employee ratings or invented approvals.
- Hermes rendered all 11 artifacts; independent render review passed. All JSON
  item text and required headings match; 82 local Markdown links resolve.
- Native Codex missing-Project replay returned no artifacts and the named blocker.
- Separate Hermes repeat invocation reported all 11 outputs as no-ops;
  independent SHA-256 comparison confirmed every artifact byte-identical.
- `farplane lint evals --changed` and `git diff --check` passed. Global skill
  validator was unavailable because it could not locate its Farplane repo root.
- Private proof folder: `weekly-unblocking.rWspaC/` under the Hermes workspace;
  `review.md` links extraction, rendering and correction logs.
- This proves bounded synthetic extraction/rendering, not live collection,
  provider delivery, installation, or performance across real company history.
