# PM Weekly semantic evals

`evals.json` starts from the frozen synthetic AGI Toy Shop bundle in
`fixtures/unblocking/` and ends at one Weekly extraction JSON. Artifact paths in
that JSON are proposed destinations; rendering and propagation belong to the
automation boundary. These case definitions and fixtures are not an operated
result or evidence that the revised skill passed.

The run is frozen at `2026-09-10T00:00:00Z`, week `2026-W37`, next week
`2026-W38`. The missing-Project case changes the inventory's Reference Catalog
memory path to an unavailable file; it reuses the same source evidence.

`expected/*.md` and the older top-level `fixtures/` W35 files are historical
Markdown-boundary examples, preserved without rewriting them to imply current
proof. They are not expected outputs for the JSON-only cases. Their names alone
do not establish exact Person IDs or workflow provenance.
